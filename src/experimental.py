"""Older experimental system, kept separate and disabled by default.

Rules to implement only in a separate future backtest:
- H1 swing break + retest
- H1 signal only when H4 EMA50 bias agrees
"""

ENABLED_BY_DEFAULT = False
